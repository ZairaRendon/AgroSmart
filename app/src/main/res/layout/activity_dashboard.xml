<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Dashboard">

    <!-- Contenido principal -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@drawable/bg_dashboard">

        <!-- Botón de menú (☰) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnMenu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="☰"
            android:textColor="@color/darkGreen"
            android:textSize="24sp"
            android:layout_gravity="start"
            android:layout_marginBottom="24dp"
            android:backgroundTint="@color/lightGreen"
            app:cornerRadius="12dp"/>

        <!-- Tarjeta principal -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="0dp"
            app:cardCornerRadius="30dp"
            app:cardElevation="16dp"
            android:background="@drawable/custom_edittext">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:padding="24dp">

                <!-- Título bienvenida -->
                <TextView
                    android:id="@+id/txtBienvenida"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="24dp"
                    android:text="Bienvenido a AgroSmart"
                    android:textColor="@color/darkGreen"
                    android:textSize="24sp"
                    android:textStyle="bold"/>

                <!-- Botones -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnCultivos"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:layout_marginBottom="12dp"
                    android:backgroundTint="@color/lightGreen"
                    android:text="Ver Cultivos"
                    android:textColor="@color/white"
                    android:textSize="18sp"
                    app:cornerRadius="20dp"/>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnSensores"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:layout_marginBottom="12dp"
                    android:backgroundTint="@color/lightGreen"
                    android:text="Ver Sensores"
                    android:textColor="@color/white"
                    android:textSize="18sp"
                    app:cornerRadius="20dp"/>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnAlertas"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:backgroundTint="@color/lightGreen"
                    android:text="Ver Predicciones"
                    android:textColor="@color/white"
                    android:textSize="18sp"
                    app:cornerRadius="20dp"/>
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- Espacio flexible -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"/>

        <!-- Logo -->
        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="274dp"
            android:layout_height="333dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="12dp"
            android:contentDescription="Logo de AgroSmart"
            android:src="@drawable/logo1"/>
    </LinearLayout>

    <!-- Barra lateral (menú) -->
    <include
        layout="@layout/barra_lateral"
        android:id="@+id/barra_lateral_root"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/bg_barralateral"/>
</androidx.drawerlayout.widget.DrawerLayout>
